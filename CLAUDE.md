# CLAUDE.md

このファイルは `jpegxs-enc` プロジェクトで作業する際にClaude Codeが従うべきルールを記載する。

## ドキュメント関連の作業ルール

実装・テストを繰り返す中で、仕様や実装計画(`PortingStrategy.txt`、
`C:\Users\gtats\.claude\plans\` 配下の計画ファイル等)に変更が生じる場合は、
以下の順序を必ず守ること:

1. **先にドキュメントを修正する**(仕様変更・計画変更の内容を反映)
2. **修正内容をユーザーに提示し、確認を取る**
3. 確認が取れてから初めて実装に着手する

ドキュメントを直さないまま実装を先行させたり、確認を飛ばして実装に進んだりしないこと。
「実装してからまとめてドキュメントに反映する」という順序は避ける。

## 対話ルール

ユーザーへの情報提示はすべて日本語で行う。思考の途中経過などもすべて日本語。
ターミナル出力での日本語以外の出力を禁止。
